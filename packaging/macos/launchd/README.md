# macOS launchd Placeholder

Future packaging work will provide:

- `com.loramapr.receiverd.plist` generation
- launchctl install/bootout helpers
- signed/notarized app/service distribution integration

For now, run receiver directly or use Linux-first packaged service path.
