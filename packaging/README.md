# Packaging

This directory tracks installation/runtime packaging assets for `loramapr-receiverd`.

Current status: scaffolding only.

Planned:

- systemd unit and default filesystem layout
- package build metadata (deb/rpm target strategy)
- container image definitions for constrained environments
