# Windows Installer Placeholder

Future expansion will add:

- MSI/EXE installer generation
- service registration via SCM
- code signing and release channel integration

Current release pipeline emits cross-compiled Windows binary archives only.
