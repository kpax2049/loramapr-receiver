{
  "name": "loramapr-receiver-lint",
  "private": true,
  "version": "0.0.0",
  "description": "Lint tooling for LoRaMapr Receiver repository scripts",
  "scripts": {
    "lint": "eslint . --max-warnings=0 --no-error-on-unmatched-pattern",
    "lint:fix": "eslint . --fix --max-warnings=0 --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "eslint": "^9.23.0",
    "globals": "^15.15.0"
  }
}
